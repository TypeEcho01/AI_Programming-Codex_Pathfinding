# BFS + DFS Pathfinding (Python Console)

Runs two built-in maps and prints:
- whether a path was found
- path length
- visited count
- rendered map overlays for visited/path tiles

## Run
```bash
python pathfinding.py
```
